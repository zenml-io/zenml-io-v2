---
import ContactForm from "../components/islands/ContactForm.tsx";
import SectionIntro from "../components/system/SectionIntro.astro";
/**
 * Startups and Academics — application form for ZenML Pro program.
 *
 * Route: /startups-and-academics
 */
import BaseLayout from "../layouts/BaseLayout.astro";
import { TURNSTILE_SITE_KEY } from "../lib/formConstants";
import {
  STARTUPS_FIELDS,
  STARTUPS_HERO,
  STARTUPS_SEO,
} from "../lib/startupsAndAcademics";
---

<BaseLayout
  title={STARTUPS_SEO.title}
  description={STARTUPS_SEO.description}
  ogTitle={STARTUPS_SEO.ogTitle}
  ogDescription={STARTUPS_SEO.ogDescription}
  surface="unified"
>
  <main>
    <section class="bg-white py-16 sm:py-24">
      <div class="mx-auto max-w-xl px-4 sm:px-6 lg:px-8">
        <SectionIntro
          align="center"
          eyebrow={STARTUPS_HERO.eyebrow}
          heading={STARTUPS_HERO.headline}
          headingLevel={1}
          description={STARTUPS_HERO.deck}
          classOverrides={{
            description: "text-lg text-gray-600",
            descriptionSpacing: "mt-3",
            wrap: "mb-8 text-center",
          }}
        />

        <ContactForm
          client:load
          formType="startup-academic"
          endpoint="/api/forms/startup-academic"
          fields={STARTUPS_FIELDS}
          submitLabel="Apply"
          loadingLabel="Submitting…"
          successMessage="Thank you for your application! We'll review it and get back to you soon."
          turnstileSiteKey={TURNSTILE_SITE_KEY}
        />
      </div>
    </section>
  </main>
</BaseLayout>
