---
import type { LabsCompareLang } from "../../../lib/labsComparison";
import LabsCodePane from "../../labs/LabsCodePane.astro";

interface Props {
  code: string;
  lang?: LabsCompareLang;
}
const { code, lang = "python" } = Astro.props;
---

<LabsCodePane label={lang} code={code} lang={lang} />
