---
---

<figure class="fig">
  <div class="panel panel--competitor">
    <div class="panel-head">
      <span class="panel-label">Pydantic AI · harness</span>
      <span class="panel-sub">How the agent thinks</span>
    </div>
    <div class="quote">
      <div class="quote-mark" aria-hidden="true">?</div>
      <div class="quote-text">
        How do I write a typed, ergonomic agent loop with first-class tools and structured outputs?
      </div>
    </div>
    <div class="concerns">
      <span class="chip">typed I/O</span>
      <span class="chip">tool calls</span>
      <span class="chip">structured outputs</span>
      <span class="chip">streaming</span>
    </div>
    <div class="foot">Scope: a single agent invocation.</div>
  </div>

  <div class="panel panel--zenml">
    <div class="panel-head">
      <span class="panel-label panel-label--accent">ZenML · orchestration</span>
      <span class="panel-sub">How the agent runs as a workload</span>
    </div>
    <div class="quote quote--accent">
      <div class="quote-mark quote-mark--accent" aria-hidden="true">?</div>
      <div class="quote-text">
        Once this agent exists, how does it run as a pipeline, pause for a human to approve, execute code in isolation, retry without re-paying, and get deployed on my infra?
      </div>
    </div>
    <div class="concerns">
      <span class="chip chip--accent">caching</span>
      <span class="chip chip--accent">sandboxes</span>
      <span class="chip chip--accent">wait / resume</span>
      <span class="chip chip--accent">artifacts</span>
      <span class="chip chip--accent">deployments</span>
    </div>
    <div class="foot foot--accent">Scope: a long-lived production workload.</div>
  </div>
</figure>

<style>
  .fig {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin: 0;
    padding: 0;
  }
  .panel {
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 16px 16px 14px;
    background: var(--color-card);
    display: flex;
    flex-direction: column;
  }
  .panel--competitor { background: var(--color-cream-50); }
  .panel--zenml { border-color: var(--color-sage-700); }

  .panel-head { margin-bottom: 14px; }
  .panel-label {
    display: block;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    color: var(--color-cream-600);
    margin-bottom: 4px;
  }
  .panel-label--accent { color: var(--color-sage-700); }
  .panel-sub {
    display: block;
    font-size: 12px;
    color: var(--color-cream-700);
  }

  .quote {
    display: flex;
    gap: 10px;
    align-items: flex-start;
    padding: 12px;
    border: 1px solid var(--color-border);
    border-radius: 8px;
    background: var(--color-card);
    margin-bottom: 10px;
  }
  .quote--accent {
    border-color: var(--color-sage-700);
    background: var(--color-sage-50);
  }
  .quote-mark {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: var(--color-cream-600);
    color: var(--color-card);
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 700;
  }
  .quote-mark--accent { background: var(--color-sage-700); }
  .quote-text {
    font-size: 12px;
    line-height: 1.5;
    color: var(--color-cream-900);
    font-style: italic;
  }

  .concerns {
    display: flex;
    flex-wrap: wrap;
    gap: 5px;
    margin-bottom: 10px;
  }
  .chip {
    display: inline-flex;
    align-items: center;
    padding: 3px 9px;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-cream-50);
    font-family: var(--font-mono);
    font-size: 10.5px;
    color: var(--color-cream-700);
  }
  .chip--accent {
    border-color: var(--color-sage-700);
    background: var(--color-sage-50);
    color: var(--color-sage-800);
  }

  .foot {
    margin-top: auto;
    font-size: 11px;
    color: var(--color-cream-600);
    font-style: italic;
  }
  .foot--accent {
    color: var(--color-sage-800);
    font-style: normal;
    font-weight: 500;
  }

  @media (max-width: 560px) {
    .fig { grid-template-columns: 1fr; }
  }
</style>
