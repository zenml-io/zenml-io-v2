---
---

<figure class="fig">
  <div class="row row--competitor">
    <div class="row-head">
      <span class="tag tag--muted">CrewAI Flow</span>
      <span class="row-sub">Stateful Flow, a re-run starts from the top</span>
    </div>
    <div class="timeline">
      <div class="step step--rerun">
        <span class="step-key">step 1</span>
        <span class="step-val">research()</span>
        <span class="step-cost">LLM</span>
      </div>
      <span class="conn">→</span>
      <div class="step step--rerun">
        <span class="step-key">step 2</span>
        <span class="step-val">plan()</span>
        <span class="step-cost">LLM</span>
      </div>
      <span class="conn">→</span>
      <div class="step step--rerun">
        <span class="step-key">step 3</span>
        <span class="step-val">draft()</span>
        <span class="step-cost">LLM</span>
      </div>
      <span class="conn">→</span>
      <div class="step step--fail">
        <span class="step-key">step 4</span>
        <span class="step-val">critique()</span>
        <span class="step-cost step-cost--fail">crash</span>
      </div>
    </div>
    <div class="row-foot">
      <span class="rerun-tag">Re-run</span>
      <span>All 4 LLM calls billed again on the next run.</span>
    </div>
  </div>

  <div class="row row--zenml">
    <div class="row-head">
      <span class="tag tag--accent">ZenML · @step</span>
      <span class="row-sub">Retry reuses the completed steps above the failure</span>
    </div>
    <div class="timeline">
      <div class="step step--cached">
        <span class="step-key">step 1</span>
        <span class="step-val">research()</span>
        <span class="step-cost step-cost--cached">reused</span>
      </div>
      <span class="conn conn--accent">→</span>
      <div class="step step--cached">
        <span class="step-key">step 2</span>
        <span class="step-val">plan()</span>
        <span class="step-cost step-cost--cached">reused</span>
      </div>
      <span class="conn conn--accent">→</span>
      <div class="step step--cached">
        <span class="step-key">step 3</span>
        <span class="step-val">draft()</span>
        <span class="step-cost step-cost--cached">reused</span>
      </div>
      <span class="conn conn--accent">→</span>
      <div class="step step--replay">
        <span class="step-key">step 4</span>
        <span class="step-val">critique()</span>
        <span class="step-cost step-cost--replay">re-runs</span>
      </div>
    </div>
    <div class="row-foot row-foot--accent">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M3 12 A9 9 0 1 0 6 5 L3 8 M3 3 L3 8 L8 8" />
      </svg>
      <span><code>zenml pipeline runs retry &lt;run&gt;</code> reads steps 1-3 from the artifact store.</span>
    </div>
  </div>
</figure>

<style>
  .fig {
    display: flex;
    flex-direction: column;
    gap: 14px;
    margin: 0;
    padding: 0;
  }
  .row {
    padding: 14px 16px;
    border-radius: 12px;
    border: 1px solid var(--color-border);
    background: var(--color-card);
  }
  .row--competitor {
    background: var(--color-cream-50);
    border-style: dashed;
  }
  .row--zenml {
    border-color: var(--color-sage-700);
  }
  .row-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 12px;
  }
  .row-sub {
    font-size: 11.5px;
    color: var(--color-cream-700);
  }
  .tag {
    display: inline-block;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    padding: 3px 8px;
    border-radius: 4px;
  }
  .tag--muted {
    background: var(--color-border);
    color: var(--color-cream-600);
  }
  .tag--accent {
    background: var(--color-sage-50);
    color: var(--color-sage-800);
  }

  .timeline {
    display: grid;
    grid-template-columns: 1fr auto 1fr auto 1fr auto 1fr;
    align-items: center;
    gap: 6px;
  }
  .step {
    display: flex;
    flex-direction: column;
    gap: 2px;
    padding: 8px 10px;
    border: 1px solid var(--color-border);
    border-radius: 8px;
    background: var(--color-card);
    min-width: 0;
  }
  .step--rerun {
    background: var(--color-cream-50);
  }
  .step--fail {
    border-color: var(--color-cream-700);
    background: var(--color-cream-100);
  }
  .step--cached {
    border-color: var(--color-border);
    background: var(--color-cream-50);
  }
  .step--replay {
    border-color: var(--color-sage-700);
    background: var(--color-sage-50);
  }
  .step-key {
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    color: var(--color-cream-600);
  }
  .step-val {
    font-family: var(--font-mono);
    font-size: 11.5px;
    color: var(--color-cream-900);
    font-weight: 600;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .step-cost {
    display: inline-block;
    margin-top: 2px;
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--color-cream-600);
    text-transform: none;
    letter-spacing: 0;
  }
  .step-cost--fail { color: var(--color-cream-700); font-weight: 700; }
  .step-cost--cached { color: var(--color-cream-700); }
  .step-cost--replay { color: var(--color-sage-700); font-weight: 700; }

  .conn {
    font-size: 14px;
    color: var(--color-cream-600);
    font-weight: 700;
    text-align: center;
  }
  .conn--accent {
    color: var(--color-sage-700);
  }

  .row-foot {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-top: 12px;
    padding-top: 10px;
    border-top: 1px solid var(--color-border);
    font-size: 11.5px;
    color: var(--color-cream-700);
  }
  .row-foot--accent {
    color: var(--color-cream-900);
  }
  .row-foot svg {
    width: 14px;
    height: 14px;
    fill: none;
    stroke: var(--color-sage-700);
    stroke-width: 1.6;
    stroke-linecap: round;
    stroke-linejoin: round;
    flex-shrink: 0;
  }
  .row-foot code {
    font-family: var(--font-mono);
    font-size: 10.5px;
    color: var(--color-cream-900);
    background: var(--color-card);
    padding: 1px 5px;
    border: 1px solid var(--color-border);
    border-radius: 3px;
  }
  .rerun-tag {
    font-size: 9.5px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    color: var(--color-cream-700);
    padding: 2px 7px;
    background: var(--color-cream-100);
    border-radius: 4px;
  }

  @media (max-width: 560px) {
    .timeline {
      grid-template-columns: 1fr;
    }
    .conn { display: none; }
  }
</style>
