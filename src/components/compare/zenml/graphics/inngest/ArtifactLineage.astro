---
---

<figure
  class="fig"
  role="group"
  aria-labelledby="artifact-lineage-title"
  aria-describedby="artifact-lineage-desc"
>
  <figcaption class="sr-only">
    <span id="artifact-lineage-title">Versioned artifacts and deployments versus event-sourced steps.</span>
    <span id="artifact-lineage-desc">Compares Inngest step memoization and replay tooling with ZenML typed artifacts, a paused wait condition, and a deployed pipeline where every request is a tracked run.</span>
  </figcaption>
  <div class="panel panel--competitor">
    <div class="panel-head">
      <span class="panel-label">Inngest · step memoization + replay UI</span>
      <span class="panel-sub">Engine persists step return values; dashboard turns it operational</span>
    </div>

    <div class="run">
      <span class="run-label">function run</span>
      <div class="steps">
        <span class="step step--done">step.run · research <span class="badge">memoized</span></span>
        <span class="step step--done">step.run · draft <span class="badge">memoized</span></span>
        <span class="step step--wait">step.wait_for_event · approve</span>
      </div>
    </div>

    <div class="tools">
      <span class="tool">replay button</span>
      <span class="tool">bulk cancel</span>
      <span class="tool">traces · alerts</span>
      <span class="tool">branch envs</span>
    </div>

    <div class="foot">Operational tools follow from durable step memoization.</div>
  </div>

  <div class="panel panel--zenml">
    <div class="panel-head">
      <span class="panel-label panel-label--accent">ZenML · artifacts + deployments</span>
      <span class="panel-sub">Typed artifacts in your bucket, a paused run you can resume</span>
    </div>

    <div class="run">
      <span class="run-label run-label--accent">run · review_pipeline</span>
      <div class="ckpts">
        <div class="ckpt">
          <span class="ckpt-name">@step research</span>
          <span class="ckpt-art">artifact: brief (v4, in your S3)</span>
        </div>
        <div class="ckpt">
          <span class="ckpt-name">@step draft</span>
          <span class="ckpt-art">artifact: draft (typed, versioned)</span>
        </div>
        <div class="ckpt ckpt--wait">
          <span class="ckpt-name">wait() · human_approval</span>
          <span class="ckpt-art">run PAUSED; resolve, then zenml pipeline runs resume</span>
        </div>
      </div>
    </div>

    <div class="deploy">
      <div class="deploy-head">
        <span class="mono">zenml pipeline deploy run.review_pipeline --name review-svc</span>
      </div>
      <div class="versions">
        <span class="ver">request</span>
        <span class="tag">= tracked run</span>
        <span class="tag tag--canary">same artifacts, same lineage</span>
      </div>
    </div>

    <div class="foot foot--accent">One record for a batch run and a served request.</div>
  </div>
</figure>

<style>
  .fig {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin: 0;
    padding: 0;
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .panel {
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 16px 16px 14px;
    background: var(--color-card);
    display: flex;
    flex-direction: column;
  }
  .panel--competitor { background: var(--color-cream-50); }
  .panel--zenml { border-color: var(--color-sage-700); }

  .panel-head { margin-bottom: 12px; }
  .panel-label {
    display: block;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    color: var(--color-cream-600);
    margin-bottom: 4px;
  }
  .panel-label--accent { color: var(--color-sage-700); }
  .panel-sub {
    font-size: 12px;
    color: var(--color-cream-700);
  }

  .run {
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 10px 12px;
    background: var(--color-card);
    margin-bottom: 10px;
  }
  .run-label {
    display: block;
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 700;
    color: var(--color-cream-900);
    margin-bottom: 8px;
  }
  .run-label--accent { color: var(--color-sage-700); }

  .steps, .ckpts {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .step {
    font-family: var(--font-mono);
    font-size: 10.5px;
    color: var(--color-cream-700);
    padding: 5px 8px;
    background: var(--color-cream-50);
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 6px;
  }
  .step--done { color: var(--color-cream-900); }
  .step--wait { color: var(--color-cream-600); font-style: italic; }
  .badge {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    color: var(--color-cream-600);
    padding: 2px 5px;
    background: var(--color-border);
    border-radius: 3px;
  }

  .ckpt {
    display: flex;
    flex-direction: column;
    gap: 2px;
    padding: 6px 9px;
    background: var(--color-cream-50);
    border-radius: 4px;
    border-left: 2px solid var(--color-sage-700);
  }
  .ckpt--wait { border-left-color: var(--color-cream-600); font-style: italic; }
  .ckpt-name {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 600;
    color: var(--color-cream-900);
  }
  .ckpt-art {
    font-size: 10px;
    color: var(--color-cream-700);
  }

  .tools {
    display: flex;
    gap: 5px;
    flex-wrap: wrap;
    margin-bottom: 8px;
  }
  .tool {
    font-size: 10px;
    color: var(--color-cream-700);
    padding: 3px 7px;
    border: 1px solid var(--color-border);
    border-radius: 4px;
    background: var(--color-card);
  }

  .deploy {
    border: 1px dashed var(--color-sage-700);
    border-radius: 8px;
    padding: 8px 10px;
    background: var(--color-sage-50);
    margin-bottom: 10px;
  }
  .deploy-head { margin-bottom: 6px; }
  .deploy-head .mono {
    font-family: var(--font-mono);
    font-size: 10.5px;
    font-weight: 700;
    color: var(--color-sage-700);
  }
  .versions {
    display: flex;
    gap: 4px;
    flex-wrap: wrap;
    align-items: center;
  }
  .ver {
    font-family: var(--font-mono);
    font-size: 10px;
    font-weight: 700;
    color: var(--color-cream-600);
    padding: 2px 6px;
    background: var(--color-card);
    border: 1px solid var(--color-border);
    border-radius: 3px;
  }
  .tag {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--color-sage-800);
    padding: 2px 6px;
    background: var(--color-card);
    border: 1px solid var(--color-sage-700);
    border-radius: 3px;
    margin-left: 4px;
  }
  .tag--canary {
    color: var(--color-cream-600);
    border-color: var(--color-border);
  }

  .foot {
    margin-top: auto;
    font-size: 11px;
    color: var(--color-cream-600);
    font-style: italic;
  }
  .foot--accent {
    color: var(--color-sage-800);
    font-style: normal;
    font-weight: 500;
  }

  @media (max-width: 820px) {
    .fig { grid-template-columns: 1fr; }
  }
</style>
