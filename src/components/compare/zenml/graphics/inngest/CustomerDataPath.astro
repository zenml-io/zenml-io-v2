---
---

<figure
  class="fig"
  role="group"
  aria-labelledby="customer-data-path-title"
  aria-describedby="customer-data-path-desc"
>
  <figcaption class="sr-only">
    <span id="customer-data-path-title">Customer-controlled data path versus managed cloud.</span>
    <span id="customer-data-path-desc">Compares Inngest Cloud as a hosted engine and dashboard with ZenML self-hosting where artifacts and compute stay in the customer cloud.</span>
  </figcaption>
  <div class="panel panel--competitor">
    <div class="panel-head">
      <span class="panel-label">Inngest · managed cloud is the easy path</span>
      <span class="panel-sub">Engine, queue, state, dashboard, hosted for you</span>
    </div>

    <div class="row row--top">
      <span class="node node--app">your app</span>
    </div>

    <svg class="conn" viewBox="0 0 100 24" preserveAspectRatio="none" aria-hidden="true">
      <path d="M50 0 L50 24 M45 18 L50 24 L55 18"/>
    </svg>

    <div class="cloud">
      <div class="cloud-head">
        <span class="mono cloud-name">Inngest Cloud</span>
        <span class="cloud-tag">SOC 2 · SAML · HIPAA BAA</span>
      </div>
      <div class="cloud-body">
        <span class="cloud-row">durable engine</span>
        <span class="cloud-row">flow control</span>
        <span class="cloud-row">replay UI</span>
      </div>
      <div class="cloud-foot">Hosted and operated by Inngest</div>
    </div>

    <div class="alt">
      <span>Alt: single-binary self-host</span>
    </div>

    <div class="foot">Spend buys managed convenience and a flow-control suite.</div>
  </div>

  <div class="panel panel--zenml">
    <div class="panel-head">
      <span class="panel-label panel-label--accent">ZenML · self-hosted, one stack abstraction</span>
      <span class="panel-sub">Pick a stack; data and compute stay in your cloud</span>
    </div>

    <div class="row row--top">
      <span class="node node--accent">your <code>@pipeline</code></span>
    </div>

    <svg class="conn" viewBox="0 0 100 24" preserveAspectRatio="none" aria-hidden="true">
      <path d="M50 0 L50 24 M45 18 L50 24 L55 18"/>
    </svg>

    <div class="server">
      <span class="server-name">ZenML server</span>
      <span class="server-sub">metadata · step state · logs · short-lived creds</span>
    </div>

    <div class="targets">
      <span class="t">your S3</span>
      <span class="t">your GCS</span>
      <span class="t">Azure Blob</span>
    </div>

    <div class="targets targets--compute">
      <span class="t t--ghost">Kubernetes</span>
      <span class="t t--ghost">Vertex AI</span>
      <span class="t t--ghost">SageMaker</span>
      <span class="t t--ghost">AzureML</span>
    </div>

    <div class="foot foot--accent">Spend buys internal platform ownership; data stays in your bucket.</div>
  </div>
</figure>

<style>
  .fig {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin: 0;
    padding: 0;
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .panel {
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 16px 16px 14px;
    background: var(--color-surface);
    display: flex;
    flex-direction: column;
  }
  .panel--competitor { background: var(--color-surface-warm); }
  .panel--zenml { border-color: var(--color-accent); }

  .panel-head { margin-bottom: 12px; }
  .panel-label {
    display: block;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-muted);
    margin-bottom: 4px;
  }
  .panel-label--accent { color: var(--color-accent); }
  .panel-sub {
    font-size: 12px;
    color: var(--color-secondary);
  }

  .row {
    display: flex;
    justify-content: center;
  }
  .node {
    display: inline-flex;
    align-items: center;
    padding: 6px 14px;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-surface);
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--color-primary);
  }
  .node--accent {
    border-color: var(--color-accent);
    color: var(--color-accent);
    background: var(--color-accent-light);
  }
  .node--accent code {
    font-family: var(--font-mono);
    color: var(--color-accent);
  }
  .node--app { color: var(--color-primary); }

  .conn {
    display: block;
    width: 100%;
    height: 18px;
    fill: none;
    stroke: var(--color-border);
    stroke-width: 1.2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  .conn path { vector-effect: non-scaling-stroke; }

  .cloud {
    border: 1px solid var(--color-muted);
    border-radius: 8px;
    padding: 10px 12px;
    background: var(--color-surface);
    margin-bottom: 8px;
  }
  .cloud-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    margin-bottom: 6px;
    gap: 8px;
  }
  .cloud-name {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 700;
    color: var(--color-primary);
  }
  .cloud-tag {
    font-size: 9px;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--color-muted);
    padding: 2px 6px;
    background: var(--color-border-light);
    border-radius: 3px;
  }
  .cloud-body {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    margin-bottom: 6px;
  }
  .cloud-row {
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--color-secondary);
    padding: 2px 6px;
    background: var(--color-bg);
    border-radius: 3px;
  }
  .cloud-foot {
    font-size: 10px;
    color: var(--color-muted);
    font-style: italic;
  }

  .alt {
    font-size: 10.5px;
    color: var(--color-muted);
    font-style: italic;
    text-align: center;
    margin-bottom: 10px;
  }

  .server {
    border: 1px dashed var(--color-border);
    border-radius: 8px;
    padding: 8px 12px;
    background: var(--color-surface);
    margin-bottom: 10px;
    display: flex;
    flex-direction: column;
    gap: 2px;
    align-items: center;
    text-align: center;
  }
  .server-name {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 700;
    color: var(--color-primary);
  }
  .server-sub {
    font-size: 10px;
    color: var(--color-muted);
  }

  .targets {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
    justify-content: center;
    margin-bottom: 6px;
  }
  .targets--compute { margin-bottom: 10px; }
  .t {
    font-family: var(--font-mono);
    font-size: 10px;
    font-weight: 700;
    color: var(--color-primary);
    padding: 4px 8px;
    border: 1px solid var(--color-accent);
    border-radius: 4px;
    background: var(--color-accent-light);
  }
  .t--ghost {
    color: var(--color-muted);
    border: 1px dashed var(--color-border);
    background: var(--color-surface);
  }

  .foot {
    margin-top: auto;
    font-size: 11px;
    color: var(--color-muted);
    font-style: italic;
  }
  .foot--accent {
    color: var(--color-accent-deep);
    font-style: normal;
    font-weight: 500;
  }

  @media (max-width: 820px) {
    .fig { grid-template-columns: 1fr; }
  }
</style>
