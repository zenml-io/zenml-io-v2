---
---

<figure
  class="fig"
  role="group"
  aria-labelledby="agent-primitives-title"
  aria-describedby="agent-primitives-desc"
>
  <figcaption class="sr-only">
    <span id="agent-primitives-title">Durable waits and dynamic graphs in Python versus polyglot durable functions.</span>
    <span id="agent-primitives-desc">Compares Inngest step primitives across several languages with ZenML dynamic pipelines, wait(), caching and step retries for Python pipelines and agents.</span>
  </figcaption>
  <div class="panel panel--competitor">
    <div class="panel-head">
      <span class="panel-label">Inngest · polyglot durable functions</span>
      <span class="panel-sub">Steps register with the engine across TypeScript, Python, Go</span>
    </div>

    <div class="langs">
      <span class="lang">TypeScript</span>
      <span class="lang">Python</span>
      <span class="lang">Go</span>
      <span class="lang lang--ghost">Kotlin/Java</span>
    </div>

    <div class="prims">
      <div class="prim">
        <span class="mono pkey">step.run</span>
        <span class="pval">durable step, return value persisted</span>
      </div>
      <div class="prim">
        <span class="mono pkey">step.sleep</span>
        <span class="pval">timer that releases compute</span>
      </div>
      <div class="prim">
        <span class="mono pkey">step.wait_for_event</span>
        <span class="pval">resume on a matching event send</span>
      </div>
      <div class="prim">
        <span class="mono pkey">step.invoke</span>
        <span class="pval">call another function across apps</span>
      </div>
    </div>

    <div class="foot">Polyglot, app-shaped. Agent telemetry is glue you write on top.</div>
  </div>

  <div class="panel panel--zenml">
    <div class="panel-head">
      <span class="panel-label panel-label--accent">ZenML · durable waits and dynamic graphs in Python</span>
      <span class="panel-sub">The agent cases that usually get a second engine, inside the pipeline</span>
    </div>

    <div class="wrap">
      <span class="mono wrap-label">@pipeline(dynamic=True)</span>
      <span class="wrap-sub">graph built from ordinary Python</span>
    </div>

    <div class="prims">
      <div class="prim prim--accent">
        <span class="mono pkey">wait()</span>
        <span class="pval">pause for a human or an external system; run marked PAUSED, resumed later</span>
      </div>
      <div class="prim">
        <span class="mono pkey">step.map()</span>
        <span class="pval">fan out over a list at run time; loops and branches are plain Python</span>
      </div>
      <div class="prim">
        <span class="mono pkey">StepRetryConfig</span>
        <span class="pval">per-step retries with delay and backoff</span>
      </div>
      <div class="prim">
        <span class="mono pkey">enable_cache</span>
        <span class="pval">unchanged steps served from cache across runs, output versioned in your bucket</span>
      </div>
    </div>

    <div class="foot foot--accent">Python only. Approvals, retries and caching without a second engine.</div>
  </div>
</figure>

<style>
  .fig {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin: 0;
    padding: 0;
  }
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .panel {
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 16px 16px 14px;
    background: var(--color-card);
    display: flex;
    flex-direction: column;
  }
  .panel--competitor { background: var(--color-cream-50); }
  .panel--zenml { border-color: var(--color-sage-700); }

  .panel-head { margin-bottom: 12px; }
  .panel-label {
    display: block;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: none;
    color: var(--color-cream-600);
    margin-bottom: 4px;
  }
  .panel-label--accent { color: var(--color-sage-700); }
  .panel-sub {
    font-size: 12px;
    color: var(--color-cream-700);
  }

  .langs {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-bottom: 10px;
  }
  .lang {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0;
    color: var(--color-cream-900);
    padding: 3px 8px;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-card);
  }
  .lang--ghost {
    color: var(--color-cream-600);
    border-style: dashed;
  }

  .wrap {
    display: inline-flex;
    align-self: flex-start;
    align-items: center;
    gap: 8px;
    padding: 6px 12px;
    border: 1px solid var(--color-sage-700);
    background: var(--color-sage-50);
    border-radius: 999px;
    margin-bottom: 10px;
  }
  .wrap-label {
    font-family: var(--font-mono);
    font-size: 12px;
    font-weight: 700;
    color: var(--color-sage-700);
  }
  .wrap-sub {
    font-size: 11px;
    color: var(--color-cream-700);
  }

  .prims {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-bottom: 10px;
  }
  .prim {
    display: flex;
    flex-direction: column;
    gap: 2px;
    padding: 8px 12px;
    border: 1px solid var(--color-border);
    border-radius: 6px;
    background: var(--color-cream-50);
  }
  .prim--accent {
    border-color: var(--color-sage-700);
    background: var(--color-sage-50);
  }
  .pkey {
    font-family: var(--font-mono);
    font-size: 11.5px;
    font-weight: 700;
    color: var(--color-cream-900);
  }
  .prim--accent .pkey { color: var(--color-sage-700); }
  .pval {
    font-size: 10.5px;
    color: var(--color-cream-700);
  }

  .foot {
    margin-top: auto;
    font-size: 11px;
    color: var(--color-cream-600);
    font-style: italic;
  }
  .foot--accent {
    color: var(--color-sage-800);
    font-style: normal;
    font-weight: 500;
  }

  @media (max-width: 820px) {
    .fig { grid-template-columns: 1fr; }
  }
</style>
