---
import LabsComparisonQuote from "../../labs/LabsComparisonQuote.astro";

interface Props {
  quote: string;
  attribution?: string;
  source?: string;
}
const { quote, attribution = "", source } = Astro.props;
---

<LabsComparisonQuote quote={quote} name={attribution} source={source} product="zenml" />
