---
import {
  KITARU_INSTALL_CMD,
  KITARU_LICENSE,
  KITARU_LINKS,
  KITARU_TRIAL_NOTE,
} from "../../lib/kitaru-landing";
import LabsButton from "../labs/LabsButton.astro";
import LabsInstallChip from "../labs/LabsInstallChip.astro";
// Cta.astro — closing CTA band. Dark grain background (KitaruGrain
// variant="dark"), headline, install chip, orange signup pill.
import { KitaruGrain } from "./islands/KitaruGrain";

const signup = {
  label: KITARU_LINKS.signup.label,
  href: KITARU_LINKS.signup.href,
  analytics: "Kitaru-CTA-Signup",
};
const install = { cmd: KITARU_INSTALL_CMD, analytics: "Kitaru-CTA-Install" };
---

<section id="cta" class="relative overflow-hidden bg-night text-night-text">
  <KitaruGrain variant="dark" client:visible class="opacity-90" />

  <div class="relative mx-auto w-full max-w-content px-gutter py-24 text-center md:py-40">
    <h2
      class="reveal font-display text-[44px] leading-[48px] text-balance text-(--color-cream-50) md:text-[72px] md:leading-[78px]"
    >
      What arrives as a complaint
      <br />
      <span class="text-ember">leaves as a regression test.</span>
    </h2>

    <div
      class="reveal mt-10 flex flex-wrap items-center justify-center gap-space-md"
      style="--reveal-delay: 120ms"
    >
      <LabsInstallChip content={install} tone="dark" />
      <LabsButton cta={signup} tone="orange" />
    </div>

    <p class="reveal mt-8 text-[13px] text-night-text/60" style="--reveal-delay: 200ms">
      {KITARU_TRIAL_NOTE} · Open source ({KITARU_LICENSE})
    </p>
    <p class="reveal mt-1.5 text-[13px] text-night-text/70" style="--reveal-delay: 240ms">
      Replays run from recordings — production never sees a test.
    </p>
  </div>
</section>
