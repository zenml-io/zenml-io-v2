---
import StepNode from "./_StepNode.astro";
import ArtifactPill from "./_ArtifactPill.astro";

/**
 * Pipelines subtab — "Same code, three destinations" (artboard 8OJ-0).
 * Left: recent pipelines list. Right: target switcher + vertical DAG with
 * fork/merge connectors illustrating data flow through artifacts.
 */

type Row = {
  initial: string;
  initialTone?: "purple" | "dark" | "purple-300";
  status?: "success" | "failed";
  name: string;
  meta: string;
  target: string;
  selected?: boolean;
};

type StepSpec = { name: string; duration: string };
type ArtifactSpec = { name: string; type: string };

interface Props {
  title: string;
  countLabel: string;
  subheader: string;
  rows: ReadonlyArray<Row>;
  fileName: string;
  rightSubhead: string;
  steps: [StepSpec, StepSpec]; // [load_data, train_model]
  inputs: [ArtifactSpec, ArtifactSpec]; // [features, labels]
  output: ArtifactSpec; // churn_predictor
}

const {
  title,
  countLabel,
  subheader,
  rows,
  fileName,
  rightSubhead,
  steps,
  inputs,
  output,
} = Astro.props;

const statusTone = (status?: Row["status"]) =>
  status === "failed"
    ? "bg-error-50 text-error-500"
    : "bg-success-50 text-success-500";
---

<div class="grid grid-cols-1 gap-3 lg:grid-cols-[360px_1fr]">
  {/* Left pane — recent pipelines */}
  <aside class="flex flex-col gap-2 rounded-md border border-gray-200 bg-white p-3">
    <div class="flex items-start justify-between gap-3 border-b border-gray-100 pb-2">
      <div class="flex flex-col gap-0.5">
        <span class="font-mono text-[11px] uppercase leading-[14px] tracking-[0.08em] text-gray-500">
          {title}
        </span>
        <span class="text-[13px] leading-4 text-gray-500">{subheader}</span>
      </div>
      <span class="font-mono text-[11px] leading-[14px] text-gray-500">
        {countLabel}
      </span>
    </div>

    <div class="flex flex-col gap-1">
      {rows.map((row) => (
        <div
          class:list={[
            "flex items-center gap-2.5 rounded-md px-2 py-2",
            row.selected && "border border-zenml-300 bg-zenml-50",
          ]}
        >
          <span
            class:list={[
              "inline-flex size-7 shrink-0 items-center justify-center rounded-md",
              statusTone(row.status),
            ]}
            aria-hidden="true"
          >
            <svg
              width="16"
              height="16"
              viewBox="0 0 24 24"
              fill="currentColor"
              xmlns="http://www.w3.org/2000/svg"
            >
              <path
                fill-rule="evenodd"
                clip-rule="evenodd"
                d="M5 3C4.44772 3 4 3.44772 4 4C4 4.55228 4.44772 5 5 5C5.55228 5 6 4.55228 6 4C6 3.44772 5.55228 3 5 3ZM2 4C2 2.34315 3.34315 1 5 1C6.30622 1 7.41746 1.83481 7.82929 3L16.1707 3C16.5825 1.83481 17.6938 1 19 1C20.6569 1 22 2.34315 22 4C22 5.65685 20.6569 7 19 7C17.6938 7 16.5825 6.16519 16.1707 5L13 5V11H16.1707C16.5825 9.83481 17.6938 9 19 9C20.6569 9 22 10.3431 22 12C22 13.6569 20.6569 15 19 15C17.6938 15 16.5825 14.1652 16.1707 13H13V15.2C13 16.0566 13.0008 16.6389 13.0376 17.089C13.0734 17.5274 13.1383 17.7516 13.218 17.908C13.4097 18.2843 13.7157 18.5903 14.092 18.782C14.2484 18.8617 14.4726 18.9266 14.911 18.9624C15.2412 18.9894 15.6425 18.997 16.171 18.9992C16.5831 17.8344 17.6941 17 19 17C20.6569 17 22 18.3431 22 20C22 21.6569 20.6569 23 19 23C17.6935 23 16.5821 22.1648 16.1705 20.9993C15.6232 20.9971 15.1515 20.9888 14.7482 20.9558C14.1861 20.9099 13.6694 20.8113 13.184 20.564C12.4314 20.1805 11.8195 19.5686 11.436 18.816C11.1887 18.3306 11.0901 17.8139 11.0442 17.2518C11 16.7106 11 16.0463 11 15.2413V5L7.82929 5C7.41746 6.16519 6.30622 7 5 7C3.34315 7 2 5.65685 2 4ZM19 3C18.4477 3 18 3.44771 18 4C18 4.55228 18.4477 5 19 5C19.5523 5 20 4.55228 20 4C20 3.44772 19.5523 3 19 3ZM19 11C18.4477 11 18 11.4477 18 12C18 12.5523 18.4477 13 19 13C19.5523 13 20 12.5523 20 12C20 11.4477 19.5523 11 19 11ZM19 19C18.4477 19 18 19.4477 18 20C18 20.5523 18.4477 21 19 21C19.5523 21 20 20.5523 20 20C20 19.4477 19.5523 19 19 19Z"
              />
            </svg>
          </span>
          <div class="flex min-w-0 flex-1 flex-col gap-0.5">
            <span
              class:list={[
                "truncate text-[13px] leading-4",
                row.selected
                  ? "font-semibold text-zenml-600"
                  : "font-semibold text-gray-900",
              ]}
            >
              {row.name}
            </span>
            <span class="truncate font-mono text-[11px] leading-[14px] text-gray-500">
              {row.meta}
            </span>
          </div>
          <span
            class:list={[
              "inline-flex shrink-0 items-center rounded-full border px-2 py-0.5 font-mono text-[10px] font-medium leading-3",
              row.selected
                ? "border-zenml-300 bg-white text-zenml-600"
                : "border-gray-200 bg-white text-gray-600",
            ]}
          >
            {row.target}
          </span>
        </div>
      ))}
    </div>
  </aside>

  {/* Right pane — DAG */}
  <section class="flex flex-col gap-3 rounded-md border border-gray-200 bg-white p-3">
    <div class="flex items-start justify-between gap-3 border-b border-gray-100 pb-2">
      <span class="text-[13px] leading-[18px] text-gray-700">
        {rightSubhead}
      </span>
      <span class="inline-flex items-center gap-1.5 rounded-full border border-gray-200 bg-white px-2.5 py-1 font-mono text-[11px] font-medium leading-[14px] text-gray-900">
        <span class="inline-block size-1.5 rounded-full bg-zenml-500" aria-hidden="true"></span>
        {fileName}
      </span>
    </div>

    {/* DAG */}
    <div class="flex flex-1 items-center justify-center rounded-md border border-gray-100 bg-cream-canvas px-4 py-3">
      <div class="flex flex-col items-center gap-0">
        {/* Step 1: load_data */}
        <StepNode name={steps[0].name} duration={steps[0].duration} width="240px" />

        {/* Fork connector */}
        <svg width="240" height="32" viewBox="0 0 240 32" aria-hidden="true">
          <path d="M120 0 L120 10 Q120 16 110 16 L60 16 Q50 16 50 22 L50 32" stroke="var(--color-gray-300)" stroke-width="1" fill="none" />
          <path d="M120 10 Q120 16 130 16 L180 16 Q190 16 190 22 L190 32" stroke="var(--color-gray-300)" stroke-width="1" fill="none" />
        </svg>

        {/* Two artifact pills side-by-side */}
        <div class="flex items-center gap-10">
          <ArtifactPill name={inputs[0].name} type={inputs[0].type} width="160px" />
          <ArtifactPill name={inputs[1].name} type={inputs[1].type} width="160px" />
        </div>

        {/* Merge connector */}
        <svg width="240" height="32" viewBox="0 0 240 32" aria-hidden="true">
          <path d="M50 0 L50 10 Q50 16 60 16 L110 16 Q120 16 120 22 L120 32" stroke="var(--color-gray-300)" stroke-width="1" fill="none" />
          <path d="M190 0 L190 10 Q190 16 180 16 L130 16 Q120 16 120 22 L120 32" stroke="var(--color-gray-300)" stroke-width="1" fill="none" />
        </svg>

        {/* Step 2: train_model */}
        <StepNode name={steps[1].name} duration={steps[1].duration} width="240px" />

        {/* Down connector */}
        <svg width="2" height="20" viewBox="0 0 2 20" aria-hidden="true">
          <line x1="1" y1="0" x2="1" y2="20" stroke="var(--color-gray-300)" stroke-width="1" />
        </svg>

        {/* Output artifact */}
        <ArtifactPill name={output.name} type={output.type} width="180px" />
      </div>
    </div>
  </section>
</div>
