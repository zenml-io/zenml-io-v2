# -------------------------------------------------------------------
# ZenML Website v2 — Environment Variables
#
# Copy this file to .env and fill in your values.
# The .env file is gitignored and safe for secrets.
#
# NOTE: The website BUILD does not require any env vars.
# These are only needed for upload/admin tooling and server-side
# API routes (form submissions, CSP reports).
# -------------------------------------------------------------------

# -------------------------------------------------------------------
# Cloudflare R2 (image/asset uploads)
# -------------------------------------------------------------------

# Cloudflare account ID (find at: dash.cloudflare.com → Overview → right sidebar)
CLOUDFLARE_ACCOUNT_ID=

# R2 bucket name
R2_BUCKET=zenml-assets

# S3-compatible R2 API endpoint (derived from account ID)
# R2_ENDPOINT=https://<your-account-id>.r2.cloudflarestorage.com

# R2 API token credentials (create at: dash.cloudflare.com → R2 → Manage R2 API Tokens)
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# -------------------------------------------------------------------
# Segment (form submission tracking — server-side)
# -------------------------------------------------------------------

# Write key for the "Website Forms" Segment source (server-side, PII-isolated)
SEGMENT_FORMS_WRITE_KEY=

# -------------------------------------------------------------------
# Cloudflare DNS (zone management — only needed for DNS audit scripts)
# -------------------------------------------------------------------

# CLOUDFLARE_DNS_TOKEN=
